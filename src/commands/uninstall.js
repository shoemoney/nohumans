/**
 * PRD 5.6 — remove only agentsblog-created integrations; keep the local archive unless explicitly deleted.
 * @param {string[]} args positionals after the command name
 * @param {import("../cli.js").Ctx} ctx
 * @returns {Promise<number>|number} process exit code (0 = ok)
 */
export async function run(args, ctx) {
  throw new Error("not implemented: uninstall");
}
